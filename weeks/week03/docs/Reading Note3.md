# Reading Note 3

This document includes Mermaid diagrams with properly escaped parentheses in node labels:

```mermaid
    graph TD;
        A[Node &lt;A&gt;] --> B[Node &lt;B&gt;];
        B --> C[Node &lt;C&gt;];
        C --> D[Node &lt;D&gt;];
```